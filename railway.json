{
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "(php artisan key:generate --force || true) && php artisan package:discover --ansi && php artisan migrate --force && (php artisan db:seed --force || echo 'Seeding skipped') && php artisan serve --host=0.0.0.0 --port=$PORT",
        "healthcheckPath": "/login",
        "restartPolicyType": "ON_FAILURE"
    }
}
